//WAP to print no. between 20 and 60
#include<stdio.h>
int main(){
    int a ;
        for (a = 0 ; a < 100 ; a++)
        if (((a > 20) && (a < 70)) && ((a >20) && (a < 60)))
        printf ("%d.\n" , a);
}
